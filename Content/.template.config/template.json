{
  "$schema": "http://json.schemastore.org/template",
  "author": "Tomasz Heimowski",
  "classifications": [ "F#", "Web", "Suave", "Fable", "Elmish", "Giraffe", "Bulma" ],
  "name": "SAFE-Stack Web App v0.3.0",
  "tags": {
    "language": "F#",
    "type": "project"
  },
  "identity": "SAFE.Template",
  "shortName": "SAFE",
  "symbols": {
    "Server": {
      "type": "parameter",
      "dataType": "choice",
      "defaultValue": "suave",
      "choices": [
        {
          "choice": "suave",
          "description": "Suave.IO - lightweight, non-blocking web server (standalone)"
        },
        {
          "choice": "giraffe",
          "description": "Giraffe - a functional micro web framework based on ASP.NET Core"
        }
      ]
    },
    "IsSuave":{
      "type": "computed",
      "value": "(Server == \"suave\")"
    },
    "Fulma": {
      "type": "parameter",
      "dataType": "bool",
      "defaultValue": "false",
      "description": "adds Fulma (Fable bindings for Bulma.io) to client"
    },
    "Remoting": {
      "type": "parameter",
      "dataType": "bool",
      "defaultValue": "false",
      "description": "adds Fable.Remoting to server and client"
    }
  },
  "sources": [
    {
      "source": "./src/Server/ProgramSuave.fs",
      "target": "./src/Server/Program.fs",
      "condition": "(Server == \"suave\")"
    },
    {
      "source": "./src/Server/ProgramGiraffe.fs",
      "target": "./src/Server/Program.fs",
      "condition": "(Server == \"giraffe\")"
    }
  ],
  "SpecialCustomOperations": {
    "**/paket.*": {
      "operations": [
        {
          "type": "conditional",
          "configuration": {
            "if": [ "#if" ],
            "else": [ "#else" ],
            "elseif": [ "#elseif" ],
            "endif": [ "#endif" ],
            "trim": "true",
            "wholeLine": "true"
          }
        }
      ]
    }
  }
}